# 4.9 Deposition Anticipated-Answer Branches — UAT checklist

**Date:** 2026-05-02
**Branch:** feat/4.9-deposition-branches
**Pre-UAT requirements:**
- Apply migration `0063_deposition_topic_branches.sql` to Supabase
  ```
  pnpm tsx scripts/apply-migrations-batch.ts 0063 0063
  ```
- Set `STRATEGY_BETA_ORG_IDS=<test-org-uuid>` in `.env.local` (already set if 4.2/4.7/4.8 ran)
- Confirm `VOYAGE_API_KEY` + `ANTHROPIC_API_KEY` set
- `bun run dev`

## Backend UAT script

```
pnpm tsx scripts/uat-4.9-deposition-branches.ts
```

Covers:
1. `generateBranchesFlow` happy path → branches row inserted with all 3 questions covered, +2cr
2. Same args → cache hit, 0cr, same row id
3. Regenerate via `regenerateSalt` → new row inserted, +2cr, both rows persist
4. Topic with 0 questions → `NoQuestionsError`, 0cr
5. Random topicId uuid → `TopicNotFoundError` (or `OutlineNotFoundError`), 0cr
6. Non-beta org → `NotBetaOrgError`, 0cr
7. Cleanup deletes outline (cascades topics + questions + branches rows)

Expected: `✅ UAT 6/6 PASSED`.

## Manual UI scenarios (run after migration + dev server)

### Anticipated answers section
- [ ] Open a deposition outline in the test org → expand a topic with ≥1 question.
- [ ] Each topic shows an "Anticipated answers (not generated)" stub with a "Generate anticipated answers (2 credits)" CTA.
- [ ] Click the CTA → 2cr charged, the section expands with one branch tree per question.
- [ ] Each tree renders 2-4 branches per question, each tagged admit/deny/evade/idk with likelihood dots and 2-3 follow-ups.
- [ ] Confidence overall shown next to the section header (low/med/high + dots).
- [ ] "Reasoning" `<details>` reveals the markdown rationale.

### Cache + regenerate
- [ ] Re-expand the section → instant render, no credit charge (cache hit by sorted-questions hash).
- [ ] Click "Regenerate (2cr)" → fresh row, +2cr.
- [ ] Old row is preserved in the DB; UI shows the latest row only.

### Staleness detection
- [ ] Edit one question's text in the outline editor.
- [ ] Re-expand the topic's anticipated answers → amber stale banner appears: "Questions changed since generation — regenerate for fresh branches."
- [ ] Click "Regenerate (2cr)" → stale badge clears, new row written, +2cr.

### Beta gate
- [ ] Non-beta org: open a deposition outline → no "Anticipated answers" section appears under any topic (UI hidden client-side).
- [ ] Direct tRPC `depositionBranches.generateBranchesForTopic` → FORBIDDEN.

### Error paths
- [ ] Topic with 0 questions → "Generate" CTA hidden (no questions to anticipate).
- [ ] Force Claude error (invalid `ANTHROPIC_API_KEY`) → error toast + 2cr refunded in ledger.

### Accessibility
- [ ] Section toggle button uses `aria-expanded` correctly.
- [ ] Likelihood dots have `aria-label="likelihood: high|med|low"`.
- [ ] Generate buttons disable while pending and show "Analyzing…".

## Notes

- Cache key = `topicId:sortedQuestionsHash:regenerateSalt`. Editing question text changes the questions hash and forces a cache miss on next call.
- Branches row stores `questions_snapshot` so the UI can detect staleness even after question text drifts.
- Reuses 4.7 opposing-counsel posture intel via `opposingCounselPostures` (latest row per case) when present.
- Local UI UAT may be blocked by an upstream voyageai package bug — recommend production smoke after merge if local dev throws on Voyage import.
- Pre-existing tsc errors (`scripts/uat-4.6-demand-letter-ai.ts`, `scripts/uat-4.7-opposing-counsel.ts`, `scripts/uat-4.8-settlement-coach.ts`, `src/server/services/stripe.ts`) unrelated to 4.9.

## Status
- [ ] Migration 0063 applied
- [ ] Backend UAT 6/6 passed
- [ ] Manual UI scenarios passed (or production smoke) → ready to merge
