# 4.8 Settlement Negotiation Coach — UAT checklist

**Date:** 2026-05-02
**Branch:** feat/4.8-settlement-coach
**Pre-UAT requirements:**
- Apply migration `0062_settlement_coach.sql` to Supabase
  ```
  pnpm tsx scripts/apply-migrations-batch.ts 0062 0062
  ```
- Set `STRATEGY_BETA_ORG_IDS=<test-org-uuid>` in `.env.local` (already set if 4.2/4.7 ran)
- Confirm `VOYAGE_API_KEY` + `ANTHROPIC_API_KEY` set
- `bun run dev`

## Backend UAT script

```
pnpm tsx scripts/uat-4.8-settlement-coach.ts
```

Covers:
1. `computeBatnaFlow` happy path → 3cr charge, BATNA row inserted with batna_low/likely/high + zopa
2. `computeBatnaFlow` same args → cache hit, 0cr
3. `computeBatnaFlow` with `damagesLikelyCents` override → new row with `has_manual_override=true`, 3cr
4. `recommendCounterFlow` against an inserted defendant pending offer ($150k) with prior plaintiff demand ($250k) → 3 variants in `variants_json`, 2cr
5. `recommendCounterFlow` same args → cache hit, 0cr
6. Non-beta org → `NotBetaOrgError`, 0cr
7. `recommendCounterFlow` for a case with no BATNA → `NeedsBatnaError`, 0cr (skipped if only one beta-org case)
8. Cleanup deletes BATNA rows (cascades counters) + injected test offers

Expected: `✅ UAT 7/7 PASSED`.

## Manual UI scenarios (run after migration + dev server)

### BATNA card (Settlement tab)
- [ ] Open a case in the test org → "Settlement" tab → BATNA card renders at top.
- [ ] First visit: card shows "Compute BATNA" CTA; click → 3cr charged, card populates with BATNA low/likely/high, ZOPA range, sensitivity table.
- [ ] Re-open tab → instant render, no credit charge (cache hit, same case_state_hash).
- [ ] Click "Recompute" with `regenerateSalt` bumped → fresh row, +3cr.

### Manual override (ComponentsEditor)
- [ ] Click "Edit components" → editor opens with extracted damages components.
- [ ] Edit one component (e.g. medical bills) → save → new BATNA row written with `has_manual_override=true`, 3cr charged.
- [ ] Sensitivity panel re-renders with the override values.
- [ ] Bounds banner appears in CounterDialog when ZOPA does not exist (no overlap).

### Counter recommender (per-offer button)
- [ ] In the offers list, a defendant offer with `response='pending'` shows a "Recommend counter" button.
- [ ] Click → dialog opens with 3 variants (aggressive/standard/conciliatory), each with rationale + risk + clamp indicator.
- [ ] 2cr charged on first compute. Re-open same dialog → cache hit, 0cr.
- [ ] Variants outside [BATNA, last_demand] show clamped badge.
- [ ] "Use this counter" prefills the existing "New counter offer" dialog with the selected amount.

### No-ZOPA case
- [ ] Compute BATNA where defendant settle_high < plaintiff BATNA → ZOPA shows "no overlap"; bounds banner visible in CounterDialog.

### Beta gate / errors
- [ ] Non-beta org: Settlement tab BATNA card shows "private beta" message; no compute button.
- [ ] Non-beta org: direct tRPC `computeBatna` → FORBIDDEN.
- [ ] No BATNA computed yet: clicking per-offer counter button shows "Compute BATNA first", no credit charge.
- [ ] Force Claude error (invalid `ANTHROPIC_API_KEY`) → error toast + 3cr (BATNA) or 2cr (counter) refunded in ledger.

### Accessibility
- [ ] Dialogs announce as modal (role=dialog + aria-modal).
- [ ] Close (✕) buttons have aria-label and disable while compute is pending.
- [ ] Bounds-clamped variants have screen-reader labels.

## Notes

- BATNA cache key = `caseId:caseStateHash:overrideHash:regenerateSalt` — caseStateHash includes latest doc + offer timestamps so adding a doc/offer invalidates BATNA.
- Counter cache key = `caseId:offerId:batnaId:offerCents:lastDemandCents:regenerateSalt`.
- Server bounds-clamps every variant to `[BATNA_likely, last_demand]`; if BATNA > last_demand the bounds collapse to `[BATNA_low, BATNA_high]` with `anyClamped=true`.
- Sensitivity table is computed server-side at fixed win-prob points (see `SENSITIVITY_WIN_POINTS` in `compute.ts`).
- Pre-existing tsc errors (`scripts/uat-4.6-demand-letter-ai.ts`, `scripts/uat-4.7-opposing-counsel.ts`, `src/server/services/stripe.ts`) unrelated to 4.8.

## Status
- [ ] Migration 0062 applied
- [ ] Backend UAT 7/7 passed
- [ ] Manual UI scenarios passed → ready to merge
